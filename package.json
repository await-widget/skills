{
  "name": "@await-widget/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:reference": "node scripts/generate-reference.mjs",
    "docs:dev": "vitepress dev docs",
    "docs:build": "npm run docs:reference && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:deploy": "npm run docs:build && wrangler pages deploy docs/.vitepress/dist --project-name await-widget-docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "wrangler": "^4.0.0"
  }
}
